1.[WebServer_ControlLEDx2](WebServer_ControlLEDx2.md)
核心功能：網頁遠端雙路 LED 燈光控制（無線開關伺服器）

詳細說明：

此功能同樣利用板載的 WiFi 建立 Web 伺服器，但主要用於「接收控制指令」。

網頁介面上會提供兩個控制按鈕，分別用來遠端控制兩顆不同顏色 LED 燈（例如藍色 LED_B 與綠色 LED_G）的開啟與關閉。

使用者只需用智慧型手機連上該網頁，點擊按鈕，就能透過網路無線切換（On/Off）開發板上的雙路 LED 狀態，是智慧家居（Smart Home）控制的基礎實作。

2.[Vibe Coding.md](https://github.com/nihil7414/nihil7414.github.io/blob/main/Vibe%20Coding.md)
核心功能：本程式碼是一個互動式的網頁前端應用，旨在為初學者（特別是高中生）提供一個視覺豐富且具備練習功能的「日文 50 音自學教室」。其核心在於透過動態特效與點擊互動，提升語言學習的趣味性與效率。

詳細說明
視覺氛圍與主題（Sakura Fall Effect）： 網頁利用 JavaScript 動態生成多個帶有漸層粉紅色的 HTML 元素，並搭配 CSS animation 動畫，模擬櫻花瓣從螢幕上方隨機飄落的視覺特效（具備旋轉與淡出效果），營造出日系學習氛圍。

50 音互動圖表網格（Syllabary Grid）： 使用 CSS Grid 佈局將日文的「平假名」與「片假名」整齊排列。每個字母卡片都綁定了點擊事件（Click Event），點擊時會觸發動態縮放回饋，並可切換顯示羅馬拼音、字體寫法或發音。

個人化字體切換（Font Toggle）： 網頁頂端整合了 Google Fonts（載入了 Noto Sans JP 與 Zen Kaku Gothic New 兩款日文字型）。使用者可以自由切換字體，以便觀察日文字母在黑體（Gothic）與標準明體間的視覺差異，加深對字形的理解。

抽卡式隨機記憶（Random Card）： 設計了一個隨機翻牌的機制。使用者點擊卡片後，系統會透過 JavaScript 的隨機函式抽取出一個 50 音字母並顯示在畫面上，作為快速閃卡（Flashcard）記憶訓練之用。

單字卡與自我測驗區塊（Vocab & Quiz）： 網頁下方延伸了語彙網格（Vocab Grid），將初學單字卡片化。同時設有隨堂測驗區（Quiz Section），點擊按鈕後會開啟測驗介面，讓學習者能即時檢驗自己的 50 音認讀成效。

3.[VibeCoding for AMB82-mini AI application](https://github.com/nihil7414/nihil7414.github.io/blob/main/VibeCoding%20for%20AMB82-mini%20AI%20application.md)
核心功能：本程式碼是一個基於 Realtek AMB82-mini 開發板 的嵌入式系統程式（Arduino C++）。它的核心功能是實現「邊緣拍照 $\rightarrow$ 雲端 AI 影像分析（年齡辨識） $\rightarrow$ 文字轉語音（TTS） $\rightarrow$ 喇叭語音播放」的一條龍智慧感知應用（Edge AI to Cloud）。

詳細說明：

硬體周邊與 Wi-Fi 初始化： 程式啟動時會先初始化硬體。透過 WiFi.begin() 連接至指定的 Wi-Fi 網路（基地台：Yokoso）。同時配置連接在開發板上的 ILI9341 LCD 螢幕，將其 SPI 通訊頻率設定為高達 40MHz（ILI9341_SPI_FREQUENCY）以確保畫面刷新的流暢度。

相機驅動與即時 LCD 顯示： 相機被設定為 VGA 畫質（640x480 的 JPEG 格式）以平衡傳輸速度與清晰度。程式呼叫 Camera.getImage() 補捉當前畫面，隨後利用 JPEGDEC 庫在暫存器中將照片尺寸等比縮小一半（JPEG_SCALE_HALF），並即時透過自訂的 JPEGDraw 回呼函式將畫面繪製到 LCD 螢幕上，作為拍攝畫面確認。

Gemini Vision 多模態 AI 運算： 程式調用 GenAI 函式庫，將暫存器中的照片數據，連同繁體中文提示詞「判斷照片中人物年齡」，透過加密通道（WiFiSSLClient）上傳至 Google Gemini 雲端平台（使用 gemini-flash-latest 模型）。AI 會分析照片中人物的面部特徵，並回傳一段描述年齡的文字結果。

Google TTS 語音合成： 收到 Gemini 的文字分析結果後，程式立即將這段文字再度發送給 Google TTS 伺服器，將其轉換為繁體中文（zh-TW）的語音訊號。

檔案儲存與 MP3 音訊播放： 程式會啟用板載的 SD 卡檔案系統（AmebaFatFS），將下載的語音訊號儲存為名為 gemini_talk.mp3 的檔案。最後調用 file.playMp3() 並設定數位音量（振幅增益設為 120），將 AI 的辨識結果以語音方式從外接喇叭播放出來。

4.[IR ranger + TFT display](https://github.com/nihil7414/nihil7414.github.io/blob/main/IR%20ranger%20%2B%20TFT%20display.md)
核心功能：紅外線測距與螢幕顯示整合

詳細說明：

此功能整合了 VL53L0X 紅外線雷射測距感測器（透過 I2C 介面）與 ILI9341 TFT 液晶螢幕（透過 SPI 介面）。

系統會透過紅外線飛行時間（ToF）技術精準測量前方物體的距離，並將量測到的距離數據即時渲染、顯示在 TFT 螢幕上。

技術修改：內容提到需要修改 Realtek 軟體包中的底層函式庫（VL53L0X.cpp），將其指定使用第二組 I2C 總線（I2C1，使用 wire1.begin() 的 SDA1 和 SCL1 腳位），以避免與其他 I2C 設備衝突。

5.[MPU6050_DMP_GetHeading](https://github.com/nihil7414/nihil7414.github.io/blob/main/MPU6050_DMP_GetHeading.md)
核心功能：取得六軸感測器的航向角（Heading Angle）

詳細說明：

此功能利用 MPU6050（整合三軸加速度計與三軸陀螺儀的晶片）來偵測物體的姿態。

它啟用了 MPU6050 內建的 DMP（數字運動處理器，Digital Motion Processor） 硬體加速功能，直接在感測器內部進行複雜的四元數與姿態解算。

最終功能是實時計算並輸出精準的航向角（Heading/Yaw，即繞 Z 軸旋轉的角度），常用於機器人導航、無人機姿態控制或平衡車。

6.[WebServer with DHT11](https://github.com/nihil7414/nihil7414.github.io/blob/main/WebServer%20with%20DHT11.md)
核心功能：網頁物聯網溫濕度監測站（環境監測 Web 伺服器）

詳細說明：

此功能結合了 DHT11 溫濕度感測器與開發板的 WiFi 網路功能。

開發板在讀取 GPIO 腳位（如 Pin 8）上的 DHT11 溫濕度數據後，會在區域網路內架設一個微型網頁伺服器（Web Server）。

當使用者用手機或電腦的瀏覽器連入開發板的 IP 位址時，網頁上就會即時顯示當前的溫度與濕度數值，達成遠端環境監控的物聯網應用。
